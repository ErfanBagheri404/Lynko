[package]
name = "lynko-core"
version = "0.1.0"
edition = "2021"
description = "Lynko shared protocol: message types, framing, capability negotiation"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
